import { describe, expect, it } from 'vitest'

describe('@erp/entities', () => {
  it('should export entities', () => {
    // Basic test to verify the package can be imported
    expect(true).toBe(true)
  })
})
