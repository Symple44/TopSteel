import { fr } from './fr'
import { en } from './en'
import { es } from './es'
import type { Translations } from '../types'

export const translations: Translations = {
  fr,
  en,
  es,
}

export { fr, en, es }